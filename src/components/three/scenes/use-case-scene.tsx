import React from "react";
import { SuperComputer } from "../assets/super-computer";

const UseCaseScene = () => {
  return (
    <group scale={9} position={[0, -2, 0]}>
      <SuperComputer />
    </group>
  );
};

export default UseCaseScene;
